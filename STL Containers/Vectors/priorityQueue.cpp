#include<iostream>
#include<queue>
using namespace std;

int main(){

    // min heap

    priority_queue<int, vector<int>, greater<int>> pq;

    // priority_queue<int> pq;

    // // maxHeap maximum value will be given highest priority;

    // pq.push(10);
    // pq.push(25);
    // pq.push(55);
    // pq.push(21);

    // cout << pq.top();

    return 0;
}